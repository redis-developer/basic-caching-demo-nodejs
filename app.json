{
  "name": "Redis Cache Example",
  "description": "Fetching number of github repos using NodeJS and Redis",
  "repository": "https://github.com/",
  "logo": "https://redis.io/images/redis-white.png",
  "keywords": ["node", "express", "redis"],
  "env": {
		"REDIS_ENDPOINT_URI": {
	  	"description": "Redis server URI",
      "required": true
    },
    "REDIS_PASSWORD": {
	    "description": "Password to your Redis server",
	    "required": false
    }
  }
}